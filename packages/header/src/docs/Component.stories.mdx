import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Header from '../Header';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';

<!-- Meta -->

<Meta title="Components/Molecules/Header" component={Header} />

export const HeaderTemplate = args => <Header {...args} />;

<!-- Canvas -->

<Story
  name="Header"
  args={{
    children: '$250 off',
  }}
  argTypes={{
    dataTestId: {
      table: {
        disable: true,
      },
    },
  }}
>
  {HeaderTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Header"
  version={version}
  stage={3}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Global-Design-System?node-id=4031%3A102387"
/>

Header component can be added in the cards. These badges can contain dynamic information, such as promotion information.

```tsx
import Header from '@heycar-uikit/header';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Header} /> },
    {
      label: 'CSS tokens',
      component: (
        <CssVars
          css={`
            ${styles}
          `}
        />
      ),
    },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
