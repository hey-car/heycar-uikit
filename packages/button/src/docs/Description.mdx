import { Container, Row, Col } from 'storybook/blocks';
import Button from '../Button';

## Variants of the button

```tsx live
const buttonVariants = ['contained', 'outlined', 'link'];

render(
  <Container>
    <Row align="large">
      {buttonVariants.map(variant => (
        <Col key={variant}>
          <Button size="large" variant={variant}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
  </Container>,
);
```

## Sizes of the button

```tsx live
const buttonSizes = ['large', 'small'];

render(
  <Container>
    <Row style={{ width: '100%' }}>
      {buttonSizes.map(size => (
        <Col span="2" key={size}>
          <Button size={size} variant="contained">
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row style={{ width: '100%' }}>
      {buttonSizes.map(size => (
        <Col span="2" key={size}>
          <Button size={size} color="primary" variant="outlined">
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row style={{ width: '100%' }}>
      {buttonSizes.map(size => (
        <Col span="2" key={size}>
          <Button size={size} variant="link">
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
  </Container>,
);
```

## Button with icons

```tsx live
const buttonSizes = ['large', 'small'];

render(
  <Container>
    <Row>
      {buttonSizes.map(size => (
        <Col key={size}>
          <Button leftIcon="🚗" color="secondary" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row>
      {buttonSizes.map(size => (
        <Col key={size}>
          <Button rightIcon="🚗" color="primary" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row>
      <Col>
        <Button color="primary" size="large">
          🚗
        </Button>
      </Col>
    </Row>
  </Container>,
);
```

## Button loading state

```tsx live
const buttonSizes = ['large', 'small'];

render(
  <Container>
    <Row>
      {buttonSizes.map(size => (
        <Col key={size}>
          <Button loading color="secondary" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row>
      {buttonSizes.map(size => (
        <Col key={size}>
          <Button loading color="primary" variant="outlined" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
  </Container>,
);
```

## Full width button

```tsx live
const buttonSizes = ['large', 'small'];

render(
  <Container>
    <Row style={{ width: '100%' }}>
      {buttonSizes.map(size => (
        <Col span="4" key={size}>
          <Button fullWidth color="secondary" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
    <Row style={{ width: '100%' }}>
      {buttonSizes.map(size => (
        <Col span="4" key={size}>
          <Button fullWidth color="primary" variant="outlined" size={size}>
            Button CTA
          </Button>
        </Col>
      ))}
    </Row>
  </Container>,
);
```
