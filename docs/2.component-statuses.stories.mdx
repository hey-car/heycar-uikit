import { Meta } from '@storybook/addon-docs';

import { ComponentHeader, Table, Status } from 'storybook/blocks';

import { version } from '../package.json';

<Meta
  title="Guidelines/Component Statuses"
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

<ComponentHeader
  name="Component Statuses"
/>

<Table
  header={['Stage', 'Status', 'Criteria']}
  rows={[
    [0, <Status stage={0} />, 'Issue with a description of the requirements for the new component'],
    [1, <Status stage={1} />, 'Component not published yet or still under implementation'],
    [2, <Status stage={2} />, 'Implemented published component, not tested in products'],
    [3, <Status stage={3} />, 'Tested in at least one of the products, clear documentation with examples of use, ready for implementation'],
  ]}
/>
