import { Meta } from '@storybook/addon-docs';

import { ComponentHeader, Table } from 'storybook/blocks';

import { version } from '../package.json';

<Meta
  title="Guidelines/Component Statuses"
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

<ComponentHeader
  name="Component Statuses"
/>

<Table
  header={['Stage', 'Status', 'Criteria']}
  rows={[
    [0, 'Proposal', 'Issue with a description of the requirements for the new component']
  ]}
/>
