import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Button from '../';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';

import CallSvg from './Call';
import MailSvg from './Mail';

<!-- Meta -->

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    children: {
      control: 'text',
    },
    Component: {
      table: {
        disable: true,
      },
    },
    dataTestId: {
      table: {
        disable: true,
      },
    },
    leftIcon: {
      table: {
        disable: true,
      },
    },
    rightIcon: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const ButtonTemplate = args => <Button {...args} />;

<!-- Canvas -->

<Story
  name="Button"
  args={{
    children: 'Button CTA',
    leftIcon: <MailSvg />,
    variant: 'contained',
    color: 'secondary',
    size: 'small',
    loading: true,
  }}
>
  {ButtonTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Button"
  version={version}
  stage={1}
  design="https://www.figma.com/"
/>

```tsx
import { Button } from '@hey-ui/button';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Button} /> },
    { label: 'CSS tokens', component: <CssVars css={styles} /> },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
