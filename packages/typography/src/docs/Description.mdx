import Typography from '../';

The **Typography** component is used to display any types of text present on the **heycar-uikit** design system.

In the design prototypes, when clicking on text, you will find a variant name like `h1`, `subheading2`, `body4`. That's all that you need to get your text component ready.
Each variant contains already the expected font size, font weight, letter spacing, line height, and text transform.

In case you want to change the HTML tag the text should be wrapped, you can set a new one with `Component`. Feel free to include customized styles with `className`.

```tsx
<Typography variant="h1">
  Your heading here!
</Typography>

<Typography variant="caption2">
  Your caption here!
</Typography>

<Typography variant="body2" Component="span">
  Your body text wrapped in `span` here!
</Typography>
```

## Typography variants

Several typography elements are available in the library. They are listed below.

- Display (`variant="display1|display2"`)
- Heading (`variant="h1|h2|h3|h4|h5|h6"`)
- Sub heading (`variant="subheading1|subheading2|subheading3|subheading4"`)
- Body (`variant="body1|body2|body3|body4"`)
- Caption (`variant="caption1|caption2|caption3|caption4|caption5"`)
- Overline (`variant="overline1|overline2"`)
- Button (`variant="button1|button2|button3|buttonLink1|buttonLink2|buttonLink3"`)

If this prop is not set, the default is `variant="body3"`. You can try the different variants in the playground below.

```tsx live
render(
  <Container>
    <Typography variant="h2">Your big heading is here 🤩</Typography>
    <br />
    <br />
    <Typography variant="subheading2">
      Your sub heading comes right after.
    </Typography>
    <br />
    <br />
    <Typography variant="body5" Component="span">
      Your body text wrapped in `span` here,⚠️followed by some more text.
    </Typography>
  </Container>,
);
```

### Typography all variants

```tsx live
render(
  <Container>
    <Typography variant="display1">Hello World | display1</Typography>
    <br />
    <Typography variant="display2">Hello World | display2</Typography>
    <br />
    <Typography variant="h1">Hello World | h1</Typography>
    <br />
    <Typography variant="h2">Hello World | h2</Typography>
    <br />
    <Typography variant="h3">Hello World | h3</Typography>
    <br />
    <Typography variant="h4">Hello World | h4</Typography>
    <br />
    <Typography variant="h5">Hello World | h5</Typography>
    <br />
    <Typography variant="h6">Hello World | h6</Typography>
    <br />
    <Typography variant="subheading1">Hello World | subheading1</Typography>
    <br />
    <Typography variant="subheading2">Hello World | subheading2</Typography>
    <br />
    <Typography variant="subheading3">Hello World | subheading3</Typography>
    <br />
    <Typography variant="subheading4">Hello World | subheading4</Typography>
    <br />
    <Typography variant="body1">Hello World | body1</Typography>
    <br />
    <Typography variant="body2">Hello World | body2</Typography>
    <br />
    <Typography variant="body3">Hello World | body3</Typography>
    <br />
    <Typography variant="body4">Hello World | body4</Typography>
    <br />
    <Typography variant="caption1">Hello World | caption1</Typography>
    <br />
    <Typography variant="caption2">Hello World | caption2</Typography>
    <br />
    <Typography variant="caption3">Hello World | caption3</Typography>
    <br />
    <Typography variant="caption4">Hello World | caption4</Typography>
    <br />
    <Typography variant="caption5">Hello World | caption5</Typography>
    <br />
    <Typography variant="overline1">Hello World | overline1</Typography>
    <br />
    <Typography variant="overline2">Hello World | overline2</Typography>
    <br />
    <Typography variant="button1">Hello World | button1</Typography>
    <br />
    <Typography variant="button2">Hello World | button2</Typography>
    <br />
    <Typography variant="button3">Hello World | button3</Typography>
    <br />
    <Typography variant="buttonLink1">Hello World | buttonLink1</Typography>
    <br />
    <Typography variant="buttonLink2">Hello World | buttonLink2</Typography>
    <br />
    <Typography variant="buttonLink3">Hello World | buttonLink3</Typography>
  </Container>,
);
```

### HTML Tags

The variant `heading` will generate an HTML tag `<h{size}>` corresponding to its size (e.g. `<h1>`, `<h2>`, etc).

The variant `caption` will generate an HTML tag `<caption>`.

The variants `body` and `display` will generate an HTML tag `<p>`.

The variants `subHeading`, `overline`, and `button` will generate `<span>` HTML tags.

If you need to customize the HTML tag, simply change it with the prop `Component`.

## Mobile versions

The heading and display variants should change properties on mobile views. This is already covered in this component.

## Customized styles and testing

In case you need to add customized styles, add a `className`. For testing purposes, the prop `dataTestId` is also available.
