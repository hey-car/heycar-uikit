import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Pagination from '../Pagination';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import { PaginationCanvas } from './Canvas.tsx';
import { PaginationItem } from '../PaginationItem';

<!-- Meta -->

<Meta
  title="Components/Pagination"
  component={Pagination}
  argTypes={{
    onClick: {
      table: {
        disable: true,
      },
    },
    locale: {
      table: {
        disable: true,
      },
    },
    renderItem: {
      table: {
        disable: true,
      },
    },
    pageLabel: {
      name: 'current page label',
      control: 'text',
      type: {
        name: 'string',
        required: false,
      },
    },
    gotoLabel: {
      name: 'page button label',
      control: 'text',
      type: { name: 'string', required: false },
    },
    prevPageLabel: {
      name: 'prev button label',
      control: 'text',
      type: { name: 'string', required: false },
    },
    nextPageLabel: {
      name: 'next button label',
      control: 'text',
      type: { name: 'string', required: false },
    },
  }}
/>

<!-- Canvas -->

<Story
  name="Pagination"
  args={{
    currentPage: 1,
    totalPages: 5,
  }}
>
  {PaginationCanvas.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Pagination"
  version={version}
  stage={1}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Global-Design-System?node-id=2073%3A48211"
/>

The pagination component allows the user to select a page within a rage.

```tsx
import Pagination from '@heycar-uikit/pagination';
*optional* import PaginationItem from '@heycar-uikit/pagination';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Pagination} /> },
    { label: 'PaginationItem Props', component: <Props of={PaginationItem} /> },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
