import { Container, Row, Col } from 'storybook/blocks';
import styles from './../styles/theme.module.css';

## Colors

Listed below are all of the color names and their following possible shades
available as CSS variables on this design system.

In order to use the colors of the **heycar-uikit** design system as
CSS variables, start the name of the variable with `--color`, followed by
the color name that you want (e.g. `-primary`, `-success`) and the shade
value (e.g. `-50`, `-600`).

If you need to use **brand colors**, simply use the variable name described right below their names on the palette below.

You can find a few examples on the code block to follow:

```css
.exampleClass {
  background-color: var(--color-primary-50);
  color: var(--color-secondary-500);
}

.anotherExampleClass {
  background: var(--color-secondary-200);
  box-shadow: 10px 5px 5px var(--color-success-400);
}

.exampleWithBrandColor {
  color: var(--color-heycar-mint);
  background: var(--color-primary-50);
  border: 1px solid var(--color-mica-blue);
}
```

export const Colors = () => {
  const shades = [
    '50',
    '100',
    '200',
    '300',
    '400',
    '500',
    '600',
    '700',
    '800',
    '900',
  ];
  const brandColors = [
    'heycar Mint',
    'Mica Blue',
    'Sunbeam Blue',
    'Championship White',
  ];
  const ColorBlock = ({ name, color }) => (
    <div className={`${styles.colorBlock}`}>
      <h3>{name}</h3>
      <div>
        {name.includes('Brand')
          ? brandColors.map(color => {
              const colorName = `${color.toLowerCase().replace(' ', '-')}`;
              return (
                <div
                  className={`${styles.colorShade} ${styles.brandColor} ${styles[colorName]}`}
                >
                  <span>{color}</span>
                  <span>--color-{colorName}</span>
                </div>
              );
            })
          : shades.map(shade => {
              const colorName = `${name.toLowerCase()}-${shade}`;
              return (
                <div className={`${styles.colorShade} ${styles[colorName]}`}>
                  <span>{shade}</span>
                </div>
              );
            })}
      </div>
    </div>
  );
  return (
    <div>
      <ColorBlock name="Brand colors" />
      <ColorBlock name="Primary" />
      <ColorBlock name="Secondary" />
      <ColorBlock name="Tertiary" />
      <ColorBlock name="Warning" />
      <ColorBlock name="Highlight" />
      <ColorBlock name="Success" />
      <ColorBlock name="Error" />
      <ColorBlock name="Neutral" />
    </div>
  );
};

<Story name="Colors">
  <Colors />
</Story>
<br />

## Shadows

There are 6 shadow options available as CSS variables on this design system.

export const Shadows = () => {
  const ShadowsBlock = ({ name }) => (
    <div className={`${styles[name]} ${styles.shadow}`}>
      <span className={`${styles.nameShadow}`}>{name}</span>
    </div>
  );
  return (
    <div className={`${styles.shadowWrapper}`}>
      <ShadowsBlock name="none" />
      <ShadowsBlock name="xs" />
      <ShadowsBlock name="s" />
      <ShadowsBlock name="m" />
      <ShadowsBlock name="xl" />
      <ShadowsBlock name="xxl" />
    </div>
  );
};

<Story name="Shadows">
  <Shadows />
</Story>
<br />

## Spacing

There are 15 spacing options available as CSS variables on this design system.

To use a spacing CSS variables, start the name of the variable with `--spacing`, followed by
the space name that you want (e.g. `-1`, `-14`).

**Note:** The spacing names are consecutive from 1 to 12, after that they go up in steps for 2

```tsx live
const row = {
  width: '100%',
  marginTop: `var(--spacing-3)`,
};
const common = {
  height: `var(--spacing-6)`,
  background: `var(--color-secondary-500)`,
};
render(
  <Container>
    <Row style={row}>
      <Col span="3">
        <h4>Name</h4>
      </Col>
      <Col span="3">
        <h4>Pixels</h4>
      </Col>
      <Col span="3"></Col>
    </Row>

    <Row style={row}>
      <Col span="3">1</Col>
      <Col span="3">4px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-1)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">2</Col>
      <Col span="3">8px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-2)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">3</Col>
      <Col span="3">12px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-3)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">4</Col>
      <Col span="3">16px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-4)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">5</Col>
      <Col span="3">20px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-5)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">6</Col>
      <Col span="3">24px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-6)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">7</Col>
      <Col span="3">28px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-7)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">8</Col>
      <Col span="3">23px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-8)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">9</Col>
      <Col span="3">36px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-9)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">10</Col>
      <Col span="3">40px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-10)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">11</Col>
      <Col span="3">44px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-11)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">12</Col>
      <Col span="3">48px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-12)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">14</Col>
      <Col span="3">56px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-14)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">16</Col>
      <Col span="3">64px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-16)`,
          }}
        />
      </Col>
    </Row>
    <Row style={row}>
      <Col span="3">18</Col>
      <Col span="3">72px</Col>
      <Col span="3">
        <div
          style={{
            ...common,
            width: `var(--spacing-18)`,
          }}
        />
      </Col>
    </Row>
  </Container>,
);
```
