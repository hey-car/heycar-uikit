import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Test from '../Test';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';

<!-- Meta -->

<Meta title="Components/Molecules/Test" component={Test} />

export const TestTemplate = args => <Test {...args} />;

<!-- Canvas -->

<Story
  name="Test"
  args={{
    children: '$250 off',
  }}
  argTypes={{
    dataTestId: {
      table: {
        disable: true,
      },
    },
  }}
>
  {TestTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Test"
  version={version}
  stage={3}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Global-Design-System?node-id=4031%3A102387"
/>

Test component can be added in the cards. These badges can contain dynamic information, such as promotion information.

```tsx
import Test from '@heycar-uikit/test';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Test} /> },
    {
      label: 'CSS tokens',
      component: (
        <CssVars
          css={`
            ${styles}${variantStyles}
          `}
        />
      ),
    },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
