import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Typography from '../';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';

export const VIEWS = ['primary', 'secondary'];
export const SIZES = ['small', 'middle', 'large'];

<Meta
  title="Components/Typography"
  component={Typography}
  argTypes={{
    children: {
      control: 'text',
      type: { name: 'string', required: false },
    },
    dataTestId: {
      table: {
        disable: true,
      },
    },
    className: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const TypographyTemplate = args => <Typography {...args} />;

<!-- Canvas -->

<Story
  name="Typography"
  args={{
    children: 'The quick brown fox jumps over the lazy dog',
    variant: 'heading',
    size: 1,
  }}
>
  {TypographyTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Typography"
  version={version}
  stage={3}
  design="https://www.figma.com/"
/>

```tsx
import { Typography } from '@hey-ui/typography';
```

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Typography} /> },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
