import { Meta, Story, Props, Preview } from '@storybook/addon-docs';
import { ComponentHeader, CssVars } from 'storybook/blocks';

import { name, version } from '../../package.json';

<Meta
  title="Guidelines/Fonts"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
      'storybook/docs/panel': { hidden: true },
    },
  }}
/>

<!-- Docs -->

<ComponentHeader
  name="Fonts"
  version={version}
  package="@heycar-uikit/fonts"
  stage={1}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Design-System"
/>

The CSS and web font files to easily self-host the heycar web fonts. All fonts are in woff/woff2 format

## Installation

To install and save in your package.json dependencies, run the command below using npm:

```bash
npm install @heycar-uikit/fonts
```

or

```bash
npm install @heycar-uikit/fonts/[200|300|500]
```

### Supported variables:

Weights: [200|300|500] will be imported only in particular weight of the font

It's as simple as importing it globally into \_app.jsx or \_app.tsx. Alternatively, you could choose to import it into a page component, however, the CSS will only be present within the page and not globally.

```
import '@heycar-uikit/fonts';


body {
  font-family: 'Objektiv';
}

p {
  font-family: 'Objektiv';
}

h1 {
  font-family: 'Objektiv';
}
```
