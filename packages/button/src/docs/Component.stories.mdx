import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Button from '../Button';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';
import sizeStyles from '!!raw-loader!../styles/size.module.css';
import variantStyles from '!!raw-loader!../styles/variant.module.css';

<!-- Meta -->

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    children: {
      control: 'text',
    },
    Component: {
      table: {
        disable: true,
      },
    },
    dataTestId: {
      table: {
        disable: true,
      },
    },
    leftIcon: {
      table: {
        disable: true,
      },
    },
    rightIcon: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const ButtonTemplate = args => <Button {...args} />;

<!-- Canvas -->

<Story
  name="Button"
  args={{
    children: 'Button CTA',
    variant: 'contained',
    color: 'primary',
    size: 'large',
    loading: false,
  }}
>
  {ButtonTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Button"
  version={version}
  stage={1}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Design-System?node-id=1111%3A116509"
/>

The Button component is used to trigger an action or event, such as submitting a form, opening a dialog, canceling an action, or performing a delete operation.

```tsx
import Button from '@hey-ui/button';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Button} /> },
    {
      label: 'CSS tokens',
      component: (
        <CssVars
          css={`
            ${styles}${sizeStyles}${variantStyles}
          `}
        />
      ),
    },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
