import { Meta, Story, Props } from '@storybook/addon-docs';
import { ComponentHeader, CssVars, TabContainer } from 'storybook/blocks';

import Chip from '../Chip';
import { version } from '../../package.json';
import Description from './Description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../styles/default.module.css';
import variantStyles from '!!raw-loader!../styles/variant.module.css';

<!-- Meta -->

<Meta
  argTypes={{
    children: {
      control: 'text',
      type: { name: 'string', required: false },
    },
  }}
  title="Components/Chip"
  component={Chip}
/>

export const ChipTemplate = args => <Chip {...args} />;

<!-- Canvas -->

<Story
  name="Chip"
  args={{
    children: 'Uunder £1,5000',
    variant: 'filter',
  }}
  argTypes={{
    dataTestId: {
      table: {
        disable: true,
      },
    },
    Component: {
      table: {
        disable: true,
      },
    },
  }}
>
  {ChipTemplate.bind({})}
</Story>

<!-- Docs -->

<ComponentHeader
  name="Chip"
  version={version}
  stage={0}
  design="https://www.figma.com/file/rrvkvQEoVTOHa7MzyKSaoz/OTP-Global-Design-System?node-id=1370%3A40520"
/>

Chips allow users to enter information, make selections, filter content, or trigger actions. While buttons are expected to appear consistently and with familiar calls to action, chips should appear dynamically as a group of multiple interactive elements.

```tsx
import Chip from '@heycar-uikit/chip';
```

<!-- Description -->

<TabContainer
  tabs={[
    { label: 'Description', component: <Description /> },
    { label: 'Props', component: <Props of={Chip} /> },
    {
      label: 'CSS tokens',
      component: (
        <CssVars
          css={`
            ${styles}${variantStyles}
          `}
        />
      ),
    },
    { label: 'Changelog', component: <Changelog /> },
  ]}
/>
